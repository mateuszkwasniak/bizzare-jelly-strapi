{
  "collectionName": "components_product_sorting_options",
  "info": {
    "displayName": "SortingOption",
    "icon": "filter",
    "description": ""
  },
  "options": {},
  "attributes": {
    "label": {
      "type": "string",
      "required": true
    },
    "field": {
      "type": "enumeration",
      "enum": [
        "name",
        "price",
        "createdAt"
      ]
    },
    "type": {
      "type": "enumeration",
      "enum": [
        "asc",
        "desc"
      ]
    }
  }
}
